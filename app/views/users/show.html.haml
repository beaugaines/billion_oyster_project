%h3 User Profile

%p= "User settings for #{@user.full_name}"

%p
  %strong Email:
  = @user.email

%p
  %strong City:
  = @user.city.present? ? @user.city : "Unlisted"
= link_to 'Edit Profile', edit_user_path(@user)

